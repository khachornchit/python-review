# given a list of chars, please do the following: 

# - change it to the next character (e.g. b -> c, m -> n)
# - after changing, if it happens to be one of 'a', 'e', 'i', 'o', 'u', please capitalize it (e.g. d -> E)
# - if it is a 'z', change back to 'A' (e.g. z -> A)

# e.g. input = ['a', 'b', 'c', 'd', 'e']
# 	output = ['b', 'c', 'd', 'E', 'f']